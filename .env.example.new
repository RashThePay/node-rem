# PostgreSQL Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=node_rem_db
POSTGRES_DB_TEST=node_rem_test_db
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password

# Remove these MongoDB variables (no longer needed)
# MONGO_URI=mongodb://localhost:27017/node-rem
# MONGO_URI_TESTS=mongodb://localhost:27017/node-rem-test

# Existing environment variables (keep these)
NODE_ENV=development
PORT=3000
JWT_SECRET=your_jwt_secret
JWT_EXPIRATION_MINUTES=15
SOCKET_ENABLED=true

# Optional services
SLACK_WEBHOOK_URL=
EMAIL_FROM_SUPPORT=
EMAIL_MAILGUN_API_KEY=
EMAIL_MAILGUN_DOMAIN=
SEC_ADMIN_EMAIL=admin@example.com
